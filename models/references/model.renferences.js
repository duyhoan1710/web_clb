let logger = require('../../logger/logger');

module.exports = (db)=>{
    const { userModel , groupModel , roleModel , userGroupRoleModel , groupRolePermissionModel , permissionModel , guestModel , newsModel , imageModel , imageNewsModel} = db;

};